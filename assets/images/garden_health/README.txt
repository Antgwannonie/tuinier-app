Garden health plant PNG's (5 stages).

The app removes white/checkerboard backgrounds automatically at runtime.

Optional — pre-process PNGs on disk:

  cd C:\Users\frede\tuinier_app
  dart run tool/prepare_garden_health_assets.dart
  flutter pub get

Then full app restart.
